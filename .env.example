# Server-side only: where next.config.ts proxies /gateway/* to. The
# Frontend has no other environment secrets - it never talks to Supabase
# or the Kernel directly, only ever to the Gateway.
GATEWAY_URL=http://localhost:3001
