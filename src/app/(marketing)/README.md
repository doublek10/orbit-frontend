Reserved for public marketing pages (pricing, about, docs index) that
don't require a session. The root landing/redirect currently lives at
`src/app/page.tsx` directly; move it here behind a proper marketing
layout once there's real public content to show.
